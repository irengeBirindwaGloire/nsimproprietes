spring:
  application:
    name: snimproprietes
  datasource:
    url: jdbc:mysql://localhost:3306/dbsnimproprietes
    username: balezi
    password: enoch
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  mvc:
    format:
      date: dd/MM/yyyy
      date-time: dd/MM/yyyy HH:mm:ss
server:
  port: 8080
